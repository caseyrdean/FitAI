# Copy to .env.local and fill in real values. Never commit .env or .env.local.
DATABASE_URL="postgresql://nutriagent:nutriagent@localhost:5432/nutriagent"
ANTHROPIC_API_KEY=""
TAVILY_API_KEY=""
UPLOAD_DIR="./uploads"

# Atlas (optional): if check-ins fail while saving full meal+workout plans, try Sonnet or raise output tokens.
# ATLAS_MODEL=claude-sonnet-4-20250514
# ATLAS_MAX_OUTPUT_TOKENS=16384
# ATLAS_MAX_HISTORY_MESSAGES=36
